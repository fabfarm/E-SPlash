import Dashboard from '../../components/Dashboard';
import Controls from '../../components/Controls';

const Home = () => {
    return (
        <main>
            <Dashboard />
            <Controls />
        </main>
    );
};

export default Home;
